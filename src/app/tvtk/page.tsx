import { DuAnDashboard } from "@/components/DuAnDashboard";

/** Phân hệ Tư vấn thiết kế — luồng hiện tại (danh mục DA / giao XN). */
export default function TvtkPage() {
  return <DuAnDashboard />;
}
